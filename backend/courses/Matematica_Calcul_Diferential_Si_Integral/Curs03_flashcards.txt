[{content={parts=[{text=--FlashCardSeparator--
Single
--InteriorSeparator--
What is the general term of a series called?
--InteriorSeparator--
Termenul general
--InteriorSeparator--
easy
--InteriorSeparator--
8
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
If the sequence of partial sums (Sn) of a series is convergent, what can be said about the series itself?
--InteriorSeparator--
(right) The series is convergent.
(wrong) The series is divergent.
(wrong) The series oscillates.
(wrong) The series is undefined.
--InteriorSeparator--
easy
--InteriorSeparator--
9
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
If lim Sn = S ∈ R, what is S called?
n→∞
--InteriorSeparator--
The sum of the series
--InteriorSeparator--
easy
--InteriorSeparator--
9
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
If a series Σan is convergent, what is the limit of its remainder of order p (Rp) as p approaches infinity?
--InteriorSeparator--
(right) 0
(wrong) Infinity
(wrong) 1
(wrong) -1
--InteriorSeparator--
medium
--InteriorSeparator--
10
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
What is the formula for the partial sum Sn of the geometric series Σq^n?
--InteriorSeparator--
(1-q^(n+1))/(1-q), if q ≠ 1; n+1, if q = 1
--InteriorSeparator--
medium
--InteriorSeparator--
11
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
For which values of q does the geometric series Σq^n converge?
--InteriorSeparator--
(right) q ∈ (-1, 1)
(wrong) q ∈ (-∞, ∞)
(wrong) q ∈ [-1, 1]
(wrong) q ∈ (0, ∞)
--InteriorSeparator--
medium
--InteriorSeparator--
11
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
What is the name given to the divergent series Σ(-1)^n?
--InteriorSeparator--
The Grandi series
--InteriorSeparator--
easy
--InteriorSeparator--
11
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
If Σan is convergent, what can be said about lim (an)?
n→∞
--InteriorSeparator--
(right) lim (an) = 0
n→∞
(wrong) lim (an) = 1
n→∞
(wrong) lim (an) = ∞
n→∞
(wrong) lim (an) = -1
n→∞
--InteriorSeparator--
medium
--InteriorSeparator--
14
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
State Cauchy's convergence criterion for series.
--InteriorSeparator--
∀ε > 0, ∃nε ∈ N*, ∀n ≥ nε, ∀p ∈ N*: |xn+1 + xn+2 + ... + xn+p| < ε
--InteriorSeparator--
hard
--InteriorSeparator--
15
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
What can be said about the harmonic series Σ(1/n)?
--InteriorSeparator--
(right) It is divergent.
(wrong) It is convergent.
(wrong) It converges to 0.
(wrong) It converges to 1.
--InteriorSeparator--
medium
--InteriorSeparator--
16
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
If Σan and Σbn are two series of real numbers, what is the series Σ(an + bn) called?
--InteriorSeparator--
The sum of the series Σan and Σbn
--InteriorSeparator--
easy
--InteriorSeparator--
17
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
If Σan = S and Σbn = T are convergent series, and λ is a real number, what is the value of Σ(λan)?
--InteriorSeparator--
(right) λS
(wrong) S + T
(wrong) λ + S
(wrong) λT
--InteriorSeparator--
medium
--InteriorSeparator--
18
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
What happens if you associate the terms of a convergent series into finite groups?
--InteriorSeparator--
You obtain a convergent series with the same sum.
--InteriorSeparator--
medium
--InteriorSeparator--
19
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
What is a sufficient condition for a series with positive terms to be convergent?
--InteriorSeparator--
(right) The sequence of its partial sums is bounded above.
(wrong) The sequence of its partial sums is unbounded.
(wrong) The terms approach infinity.
(wrong) The terms oscillate.
--InteriorSeparator--
medium
--InteriorSeparator--
21
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
State the first comparison test (CCI) for series with positive terms.
--InteriorSeparator--
If 0 ≤ an ≤ bn for all n ∈ N*, then if Σbn converges, Σan converges; and if Σan diverges, Σbn diverges.
--InteriorSeparator--
hard
--InteriorSeparator--
22
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
State the second comparison test (CCII) for series with positive terms.
--InteriorSeparator--
(right) If an > 0, bn > 0, and an+1/an ≤ bn+1/bn, then if Σbn converges, Σan converges and if Σan diverges, Σbn diverges.
(wrong) If an > 0, bn > 0, and an+1/an ≥ bn+1/bn, then if Σbn converges, Σan diverges and if Σan diverges, Σbn converges.
(wrong) If an < 0, bn < 0, and an+1/an ≤ bn+1/bn, then if Σbn converges, Σan converges and if Σan diverges, Σbn diverges.
(wrong) If an > 0, bn > 0, and an+1/an ≤ bn+1/bn, then if Σbn diverges, Σan converges and if Σan converges, Σbn diverges.
--InteriorSeparator--
hard
--InteriorSeparator--
23
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
State the limit comparison test (CCIII) for series.
--InteriorSeparator--
If lim (an/bn) = L ∈ R+, then: i) if L ∈ (0,+∞), then Σan and Σbn have the same nature; ii) if L = 0, then a) if Σbn converges, Σan converges; b) if Σan diverges, Σbn diverges; iii) if L = +∞, then a) if Σan converges, Σbn converges; b) if Σbn diverges, Σan diverges.
--InteriorSeparator--
hard
--InteriorSeparator--
24
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
State Cauchy's condensation test.
--InteriorSeparator--
(right) If (xn) is a decreasing sequence of positive numbers, then Σxn and Σ2^n * x2^n have the same nature.
(wrong) If (xn) is an increasing sequence of positive numbers, then Σxn converges if and only if Σ2^n * x2^n converges.
(wrong) If (xn) is a positive sequence, then Σxn converges if and only if Σn * xn converges.
(wrong) If (xn) is a decreasing sequence of positive numbers, then Σxn converges if and only if Σ(1/2)^n * x(1/2)^n converges.
--InteriorSeparator--
hard
--InteriorSeparator--
26
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
For which values of α does the generalized harmonic series Σ(1/n^α) converge?
--InteriorSeparator--
α > 1
--InteriorSeparator--
medium
--InteriorSeparator--
27
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
State Cauchy's root test.
--InteriorSeparator--
(right) If lim (an^(1/n)) = L, then: i) if L < 1, Σan converges; ii) if L > 1, Σan diverges; iii) if L = 1, the test is inconclusive.
(wrong) If lim (an^(1/n)) = L, then: i) if L ≤ 1, Σan converges; ii) if L > 1, Σan diverges.
(wrong) If lim (an^(1/n)) = L, then: i) if L < 1, Σan diverges; ii) if L > 1, Σan converges; iii) if L = 1, the test is inconclusive.
(wrong) If lim (an^(1/n)) = L, then: i) if L < 1, Σan converges; ii) if L ≥ 1, Σan diverges.
--InteriorSeparator--
hard
--InteriorSeparator--
28
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
State Kummer's Test.
--InteriorSeparator--
Given a series Σan, with an > 0, and a sequence (An), if lim [An(an/an+1) - An+1] = L, then: i) if L > 0, Σan converges; ii) if L < 0 and Σ(1/An) diverges, then Σan diverges.
--InteriorSeparator--
hard
--InteriorSeparator--
29
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
State D'Alembert's ratio test.
--InteriorSeparator--
(right) If lim (an+1/an) = L, then: i) if L < 1, Σan converges; ii) if L > 1, Σan diverges; iii) if L = 1, the test is inconclusive.
(wrong) If lim (an/an+1) = L, then: i) if L < 1, Σan converges; ii) if L > 1, Σan diverges; iii) if L = 1, the test is inconclusive.
(wrong) If lim (an+1/an) = L, then: i) if L < 1, Σan diverges; ii) if L > 1, Σan converges; iii) if L = 1, the test is inconclusive.
(wrong) If lim (an/an+1) = L, then: i) if L < 1, Σan diverges; ii) if L > 1, Σan converges; iii) if L = 1, the test is inconclusive.
--InteriorSeparator--
hard
--InteriorSeparator--
30
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
State Raabe-Duhamel's test.
--InteriorSeparator--
If lim [n(an/an+1 - 1)] = p, then: i) if p > 1, Σan converges; ii) if p < 1, Σan diverges; iii) if p = 1, the test is inconclusive.
--InteriorSeparator--
hard
--InteriorSeparator--
31
--FlashCardSeparator--

--FlashCardSeparator--
Multiple
--InteriorSeparator--
State Bertrand's test.
--InteriorSeparator--
(right) If lim [ln(n) * (n*an/an+1 - (n+1))] = p, then: i) if p > 0, Σan converges; ii) if p < 0, Σan diverges; iii) if p = 0, the test is inconclusive.
(wrong) If lim [ln(n) * (an/an+1 - (n+1))] = p, then: i) if p > 0, Σan converges; ii) if p < 0, Σan diverges; iii) if p = 0, the test is inconclusive.
(wrong) If lim [ln(n) * (n*an/an+1 - (n+1))] = p, then: i) if p > 0, Σan diverges; ii) if p < 0, Σan converges; iii) if p = 0, the test is inconclusive.
(wrong) If lim [ln(n) * (an/an+1 - (n+1))] = p, then: i) if p > 0, Σan diverges; ii) if p < 0, Σan converges; iii) if p = 0, the test is inconclusive.
--InteriorSeparator--
hard
--InteriorSeparator--
32
--FlashCardSeparator--

--FlashCardSeparator--
Single
--InteriorSeparator--
State Gauss's test.
--InteriorSeparator--
If an/an+1 = 1 + a/n + β/n^2 + yn/n^2 where (yn) is bounded, then: i) if a > 1, Σan converges; ii) if a < 1, Σan diverges; iii) if a = 1 and β > 1, Σan converges; iv) if a = 1 and β ≤ 1, Σan diverges.
--InteriorSeparator--
hard
--InteriorSeparator--
33
--FlashCardSeparator--
}], role=model}, finishReason=STOP, avgLogprobs=-0.06017598547704954}]